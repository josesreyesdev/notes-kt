package instrumentedTest

//import androidx.test.ext.junit.runners.AndroidJUnit4
import org.junit.Rule
import org.junit.Test
import org.junit.runner.RunWith

//@RunWith(AndroidJUnit4::class)
class ExampleInstrumentedTest {
    /*
    //Iniciar la actividad especificada por el desarrollador
    @get:Rule() //indica que debe ejecutarse antes de cada prueba de la clase
    val activity = ActivityScenarioRule(MainActivity::class.java) //ActivityScenarioRule proviene de la biblioteca AndroidX

    @Test
    fun calculate_20_percent_tip() {
        onView(withId(R.id.cost_of_service_edit_text))
            .perform( typeText("50.00"))

        onView(withId(R.id.calculate))
            .perform( click())

        onView(withId(R.id.tip_result))
            .check( matches(withText(containsString("$10.00"))))

    } */
}